services:
  - type: web
    name: shoplex-frontend
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npx serve -c serve.json -l 10000
    envVars:
      - key: NODE_ENV
        value: production