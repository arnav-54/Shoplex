services:
  - type: web
    name: shoplex-frontend
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npx serve -s dist -p 10000
    envVars:
      - key: NODE_ENV
        value: production